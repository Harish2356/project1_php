create database MobileStoreCreator;

use MobileStoreCreator;


Create table mobiles ( mob_id int not null auto_increment, mobile_name varchar(50), price varchar(10), version varchar(10), brand varchar(20), primary key(mob_id));

select * from mobiles;
